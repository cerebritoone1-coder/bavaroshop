{
  "deploy": {
    "preDeployCommand": "python manage.py collectstatic --noinput",
    "startCommand": "python manage.py migrate && /opt/venv/bin/gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
  }
}