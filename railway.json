{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "source venv/bin/activate && python manage.py migrate && python manage.py collectstatic --noinput && python manage.py createsuperuser --noinput --username cerebritoone1 --email cerebritoone1@gmail.com && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
  }
}